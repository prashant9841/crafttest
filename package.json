{
	"dependencies": {
		"bootstrap": "^5.0.0-alpha1",
		"node-sass": "^6.0.1",
		"slick-slider": "^1.8.2"
	},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"scss": "node-sass --watch src -o web"
	}
}
